#import <UIKit/UIKit.h>

/// Shared visual base for NeoWC-owned list pages. It deliberately does not
/// affect WeChat's native table views.
@interface NeoWCCardTableViewController : UITableViewController
@end
