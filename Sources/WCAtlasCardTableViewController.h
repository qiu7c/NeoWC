#import <UIKit/UIKit.h>

/// Shared visual base for WCAtlas-owned list pages. It deliberately does not
/// affect WeChat's native table views.
@interface WCAtlasCardTableViewController : UITableViewController
@end
